# ============================================================================
# CI/CD Pipeline for FREDDY - Personal & Authentication Services Server
# ============================================================================
# Personal home server running photos, cloud storage, home automation
# Repository: nuniesmith/freddy
#
# Services: nginx, photoprism, nextcloud, homeassistant, audiobookshelf
#
# Features:
#   - Cloudflare DNS management (update A records to Tailscale IPs)
#   - SSL certificate generation via Certbot with Cloudflare DNS challenge
#   - Deployment via Tailscale + SSH
# ============================================================================

name: 🏠 Freddy Deploy

on:
    push:
        branches:
            - main
        paths-ignore:
            - "**.md"
            - "docs/**"
            - ".gitignore"
    pull_request:
        branches: [main]
    workflow_dispatch:
        inputs:
            skip_deploy:
                description: "Skip deployment (test workflow only)"
                required: false
                type: boolean
                default: false
            update_dns:
                description: "Update Cloudflare DNS records"
                required: false
                type: boolean
                default: true
            force_ssl_regen:
                description: "Force SSL certificate regeneration (fixes corrupted certs)"
                required: false
                type: boolean
                default: false
            use_staging_certs:
                description: "Use Let's Encrypt staging server (for testing, avoids rate limits)"
                required: false
                type: boolean
                default: false
    schedule:
        # Run SSL renewal check weekly on Sundays at 3am UTC
        - cron: "0 3 * * 0"

permissions:
    contents: read

env:
    SERVER_NAME: freddy
    PROJECT_PATH: ~/freddy
    DOMAIN: 7gram.xyz

    # Freddy DNS records (services hosted on freddy)
    FREDDY_DNS_RECORDS: >-
        7gram.xyz
        *.7gram.xyz
        nc.7gram.xyz
        photo.7gram.xyz
        home.7gram.xyz
        abs.7gram.xyz

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

defaults:
    run:
        shell: bash

jobs:
    # ==========================================================================
    # CLOUDFLARE DNS UPDATE
    # ==========================================================================
    dns-update:
        name: 🌐 Update Cloudflare DNS
        runs-on: ubuntu-latest
        timeout-minutes: 10
        if: |
            github.event_name == 'workflow_dispatch' && inputs.update_dns ||
            github.event_name == 'push' ||
            github.event_name == 'schedule'

        outputs:
            dns-updated: ${{ steps.dns-freddy.outputs.updated }}

        steps:
            - name: 📥 Checkout code
              uses: actions/checkout@v4

            - name: 🌐 Update Freddy DNS Records
              id: dns-freddy
              uses: nuniesmith/actions/.github/actions/cloudflare-dns-update@main
              with:
                  api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
                  zone-id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
                  record-name: ${{ env.DOMAIN }}
                  record-content: ${{ secrets.FREDDY_TAILSCALE_IP }}
                  record-type: A
                  ttl: "1"
                  proxied: "false"
                  additional-records: |
                      [
                        {"name": "nc.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "photo.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "home.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "abs.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "freddy.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"}
                      ]

            - name: 🌐 Update Sullivan DNS Records (proxied via Freddy nginx)
              id: dns-sullivan
              uses: nuniesmith/actions/.github/actions/cloudflare-dns-update@main
              with:
                  api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
                  zone-id: ${{ secrets.CLOUDFLARE_ZONE_ID }}
                  record-name: sullivan.7gram.xyz
                  record-content: ${{ secrets.FREDDY_TAILSCALE_IP }}
                  record-type: A
                  ttl: "1"
                  proxied: "false"
                  additional-records: |
                      [
                        {"name": "jellyfin.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "emby.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "plex.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "sonarr.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "radarr.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "lidarr.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "jackett.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "qbt.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "calibre.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "yt.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "filebot.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "grocy.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "mealie.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "wiki.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"},
                        {"name": "duplicati.7gram.xyz", "content": "${{ secrets.FREDDY_TAILSCALE_IP }}"}
                      ]

            - name: 📋 DNS Update Summary
              run: |
                  echo "## 🌐 DNS Update Summary" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "**Target IP (Freddy Tailscale):** \`${{ secrets.FREDDY_TAILSCALE_IP }}\`" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "All DNS records for 7gram.xyz have been updated to point to the Freddy server." >> $GITHUB_STEP_SUMMARY
                  echo "Freddy's nginx will reverse proxy Sullivan services over Tailscale." >> $GITHUB_STEP_SUMMARY

    # ==========================================================================
    # DEPLOYMENT (includes SSL generation)
    # ==========================================================================
    deploy:
        name: 🚀 Deploy to Freddy
        runs-on: ubuntu-latest
        timeout-minutes: 20
        needs: [dns-update]
        if: |
            always() &&
            (needs.dns-update.result == 'success' || needs.dns-update.result == 'skipped') &&
            (
                github.event_name == 'push' ||
                github.event_name == 'schedule' ||
                (github.event_name == 'workflow_dispatch' && !inputs.skip_deploy)
            )

        steps:
            - name: 📥 Checkout code
              uses: actions/checkout@v4

            # ======================================================================
            # TAILSCALE CONNECTION
            # ======================================================================
            - name: 🔌 Connect to Tailscale
              id: tailscale
              uses: nuniesmith/actions/.github/actions/tailscale-connect@main
              with:
                  oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
                  oauth-secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
                  target-ip: ${{ secrets.FREDDY_TAILSCALE_IP }}
                  target-ssh-port: ${{ secrets.SSH_PORT || '22' }}

            # ======================================================================
            # SSL CERTIFICATE GENERATION
            # ======================================================================
            - name: 🧹 Clean corrupted SSL certificates
              if: inputs.force_ssl_regen == true
              run: |
                  echo "🧹 Forcing comprehensive SSL certificate cleanup..."
                  echo "This will remove ALL existing certificates from host filesystem"

                  # Determine which SSH key and user to use
                  if [ -n "$ROOT_SSH_KEY" ]; then
                    echo "🔑 Using root SSH key for SSL cleanup"
                    CLEANUP_SSH_KEY="$ROOT_SSH_KEY"
                    CLEANUP_USER="root"
                    SUDO_CMD=""
                  else
                    echo "🔑 Using actions SSH key with sudo for cleanup"
                    CLEANUP_SSH_KEY="$SSH_KEY"
                    CLEANUP_USER="${{ secrets.SSH_USER || 'actions' }}"
                    SUDO_CMD="sudo"
                  fi

                  # Setup SSH key
                  mkdir -p ~/.ssh
                  echo "$CLEANUP_SSH_KEY" > ~/.ssh/id_rsa
                  chmod 600 ~/.ssh/id_rsa

                  ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa -p ${{ secrets.SSH_PORT || '22' }} \
                    $CLEANUP_USER@${{ secrets.FREDDY_TAILSCALE_IP }} << 'ENDSSH'

                  set -e
                  echo "================================================"
                  echo "  SSL CERTIFICATE CLEANUP"
                  echo "================================================"
                  echo "👤 Running as: $(whoami)"

                  # Determine sudo command (empty if root)
                  if [ "$(whoami)" = "root" ]; then
                    SUDO_CMD=""
                  else
                    SUDO_CMD="sudo"
                  fi

                  # Remove certificates from host filesystem
                  echo "→ Removing certificates from /etc/letsencrypt..."
                  $SUDO_CMD rm -rf /etc/letsencrypt/live
                  $SUDO_CMD rm -rf /etc/letsencrypt/archive
                  $SUDO_CMD rm -rf /etc/letsencrypt/renewal
                  $SUDO_CMD mkdir -p /etc/letsencrypt

                  # Set proper permissions
                  $SUDO_CMD chmod 755 /etc/letsencrypt

                  echo ""
                  echo "✅ Complete cleanup finished"
                  echo "   Ready for fresh SSL certificate generation"
                  echo "================================================"
                  ENDSSH

                  # Cleanup SSH key
                  rm -f ~/.ssh/id_rsa
              env:
                  SSH_KEY: ${{ secrets.SSH_KEY }}
                  ROOT_SSH_KEY: ${{ secrets.ROOT_SSH_KEY }}

            - name: 🔐 Generate SSL Certificates (Let's Encrypt + Cloudflare DNS)
              id: certbot
              uses: nuniesmith/actions/.github/actions/ssl-certbot-cloudflare@main
              with:
                  domain: ${{ env.DOMAIN }}
                  # Note: Don't include specific subdomains that are covered by wildcards
                  # *.7gram.xyz covers nc, photo, home, abs, sullivan, etc.
                  # *.sullivan.7gram.xyz covers any sullivan subdomains
                  additional-domains: "*.7gram.xyz,*.sullivan.7gram.xyz"
                  cloudflare-api-token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
                  email: ${{ secrets.SSL_EMAIL }}
                  propagation-seconds: 60
                  staging: ${{ inputs.use_staging_certs || false }}
                  fallback-to-self-signed: true
                  deploy-to-server: true
                  ssh-host: ${{ secrets.FREDDY_TAILSCALE_IP }}
                  ssh-port: ${{ secrets.SSH_PORT || '22' }}
                  ssh-user: ${{ secrets.SSH_USER || 'actions' }}
                  ssh-key: ${{ secrets.SSH_KEY }}
                  root-ssh-key: ${{ secrets.ROOT_SSH_KEY }}
                  use-sudo: false
                  deploy-method: host-path
                  host-cert-path: /etc/letsencrypt

            - name: ✅ Verify SSL certificates on host
              run: |
                  echo "✅ Verifying certificates on host filesystem..."

                  # Determine which SSH key and user to use
                  if [ -n "$ROOT_SSH_KEY" ]; then
                    echo "🔑 Using root SSH key for verification"
                    VERIFY_SSH_KEY="$ROOT_SSH_KEY"
                    VERIFY_USER="root"
                    SUDO_CMD=""
                  else
                    echo "🔑 Using actions SSH key for verification"
                    VERIFY_SSH_KEY="$SSH_KEY"
                    VERIFY_USER="${{ secrets.SSH_USER || 'actions' }}"
                    SUDO_CMD="sudo"
                  fi

                  # Setup SSH key
                  mkdir -p ~/.ssh
                  echo "$VERIFY_SSH_KEY" > ~/.ssh/id_rsa
                  chmod 600 ~/.ssh/id_rsa

                  ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa -p ${{ secrets.SSH_PORT || '22' }} \
                    $VERIFY_USER@${{ secrets.FREDDY_TAILSCALE_IP }} << 'ENDSSH'

                  echo "================================================"
                  echo "  SSL CERTIFICATE VERIFICATION"
                  echo "================================================"
                  echo "👤 Running as: $(whoami)"

                  # Determine sudo command (empty if root)
                  if [ "$(whoami)" = "root" ]; then
                    SUDO_CMD=""
                  else
                    SUDO_CMD="sudo"
                  fi

                  # Check if certificates exist on host filesystem
                  if [ -f /etc/letsencrypt/live/7gram.xyz/fullchain.pem ]; then
                    echo "✓ Let's Encrypt certificates found on host filesystem"

                    # List certificate files
                    echo ""
                    echo "Certificate files in /etc/letsencrypt/live/7gram.xyz/:"
                    $SUDO_CMD ls -lah /etc/letsencrypt/live/7gram.xyz/

                    # Verify certificate details
                    echo ""
                    echo "Certificate details:"
                    $SUDO_CMD openssl x509 -in /etc/letsencrypt/live/7gram.xyz/fullchain.pem -noout -issuer -dates -subject || true

                    # Verify certificate and private key match (supports both RSA and ECDSA)
                    CERT_HASH=$($SUDO_CMD openssl x509 -noout -pubkey -in /etc/letsencrypt/live/7gram.xyz/fullchain.pem 2>/dev/null | openssl sha256 || echo "cert_error")
                    KEY_HASH=$($SUDO_CMD openssl pkey -pubout -in /etc/letsencrypt/live/7gram.xyz/privkey.pem 2>/dev/null | openssl sha256 || echo "key_error")

                    if [ "$CERT_HASH" = "$KEY_HASH" ] && [ "$CERT_HASH" != "cert_error" ]; then
                      echo "✓ Certificate and private key match"
                    else
                      echo "⚠️ Certificate verification inconclusive (may be ECDSA key)"
                      echo "✓ Certificate files exist and are readable"
                    fi
                  else
                    echo "⚠️ No Let's Encrypt certificates found on host"
                    echo "   Checking if directory exists..."
                    $SUDO_CMD ls -la /etc/letsencrypt/ 2>/dev/null || echo "   /etc/letsencrypt does not exist"
                  fi

                  echo "================================================"
                  ENDSSH

                  # Cleanup SSH key
                  rm -f ~/.ssh/id_rsa
              env:
                  SSH_KEY: ${{ secrets.SSH_KEY }}
                  ROOT_SSH_KEY: ${{ secrets.ROOT_SSH_KEY }}

            # ======================================================================
            # DEPLOY VIA SSH
            # ======================================================================
            - name: 🚀 Deploy via SSH
              id: deploy
              if: steps.tailscale.outputs.connected == 'true'
              uses: nuniesmith/actions/.github/actions/ssh-deploy@main
              with:
                  host: ${{ secrets.FREDDY_TAILSCALE_IP }}
                  port: ${{ secrets.SSH_PORT || '22' }}
                  username: ${{ secrets.SSH_USER || 'actions' }}
                  ssh-key: ${{ secrets.SSH_KEY }}
                  project-path: ${{ env.PROJECT_PATH }}
                  git-pull: false
                  git-branch: ${{ github.ref_name }}
                  docker-pull: false
                  docker-prune: false
                  pre-deploy-command: |
                      echo "🏠 Deploying to Freddy server..."

                      # Create project directory if it doesn't exist
                      echo "📁 Ensuring project directory exists..."
                      mkdir -p ${{ env.PROJECT_PATH }}

                      # Stop existing services to release file locks
                      echo "🛑 Stopping existing services..."
                      if [ -d "${{ env.PROJECT_PATH }}" ]; then
                        cd ${{ env.PROJECT_PATH }}
                        ./run.sh stop 2>/dev/null || docker compose down 2>/dev/null || true
                        cd ~
                      fi

                      # ============================================================
                      # COMPREHENSIVE SSL CERTIFICATE CLEANUP
                      # ============================================================
                      echo ""
                      echo "🧹 Cleaning up old/corrupted SSL certificates..."

                      # Stop and remove nginx container specifically
                      echo "  → Stopping nginx container..."
                      docker stop nginx 2>/dev/null || true
                      docker rm -f nginx 2>/dev/null || true

                      # Remove ssl-certs Docker volume (forces fresh cert generation)
                      echo "  → Removing ssl-certs Docker volume..."
                      docker volume rm ssl-certs 2>/dev/null || true

                      # Remove any dangling certificate-related volumes
                      echo "  → Removing dangling volumes..."
                      docker volume prune -f 2>/dev/null || true

                      echo "✅ SSL cleanup complete - ready for fresh certificates"
                      echo ""
                      # ============================================================

                      # Handle git repository setup
                      cd ${{ env.PROJECT_PATH }}

                      if [ -d .git ] && git status >/dev/null 2>&1; then
                        echo "📥 Updating existing repository..."
                        git fetch origin
                        git checkout ${{ github.ref_name }}
                        git pull origin ${{ github.ref_name }} || {
                          echo "⚠️ Git pull failed, doing hard reset..."
                          git reset --hard origin/${{ github.ref_name }}
                        }
                      else
                        echo "📥 Cloning repository (first deployment)..."
                        cd ~
                        rm -rf ${{ env.PROJECT_PATH }}
                        git clone https://github.com/${{ github.repository }}.git ${{ env.PROJECT_PATH }}
                        cd ${{ env.PROJECT_PATH }}
                        git checkout ${{ github.ref_name }}
                      fi

                      # Ensure run.sh is executable
                      chmod +x ./run.sh 2>/dev/null || true

                      # Verify SSL certificates are ready on host filesystem
                      echo "🔍 Checking for SSL certificates on host filesystem..."

                      # Check if certificates exist on host
                      if [ -f /etc/letsencrypt/live/${{ env.DOMAIN }}/fullchain.pem ]; then
                        echo "✅ Let's Encrypt certificates found on host at /etc/letsencrypt"
                        ls -lah /etc/letsencrypt/live/${{ env.DOMAIN }}/

                        # Verify certificate and key match
                        echo "🔐 Verifying certificate/key pair..."
                        CERT_HASH=$(openssl x509 -noout -pubkey -in /etc/letsencrypt/live/${{ env.DOMAIN }}/fullchain.pem 2>/dev/null | openssl sha256 || echo "cert-error")
                        KEY_HASH=$(openssl pkey -pubout -in /etc/letsencrypt/live/${{ env.DOMAIN }}/privkey.pem 2>/dev/null | openssl sha256 || echo "key-error")

                        if [ "$CERT_HASH" = "$KEY_HASH" ] && [ "$CERT_HASH" != "cert-error" ]; then
                          echo "✅ Certificate and private key match!"
                          echo "✅ Nginx will use Let's Encrypt production certificates"
                        else
                          echo "⚠️ Certificate verification inconclusive (may be ECDSA key)"
                          echo "✅ Certificate files exist - nginx will use them"
                        fi
                      else
                        echo "⚠️  No Let's Encrypt certificates found on host"
                        echo "   Nginx will start using self-signed fallback certificates"
                        echo "   SSL will work but browsers will show security warnings"
                        echo ""
                        echo "   To obtain Let's Encrypt certificates:"
                        echo "   • Run this workflow with 'force_ssl_regen' enabled"
                        echo "   • Or wait for the weekly scheduled renewal"
                      fi
                      echo ""

                      # Load environment variables
                      if [ -f .env ]; then
                        set -a && source .env && set +a
                        echo "✓ Environment loaded"
                      fi

                  deploy-command: |
                      cd ${{ env.PROJECT_PATH }}

                      # Stop existing services
                      echo "🐳 Stopping existing services..."
                      if [ -x "./run.sh" ]; then
                        ./run.sh stop 2>/dev/null || true
                      else
                        docker compose down 2>/dev/null || true
                      fi

                      # Rebuild nginx with --no-cache to pick up fresh certificates
                      echo "🔨 Rebuilding nginx container with fresh certificates..."
                      docker compose build --no-cache nginx

                      # Pull other images
                      echo "📥 Pulling latest images for other services..."
                      docker compose pull --ignore-pull-failures

                      # Start all services
                      echo "🚀 Starting all services..."
                      docker compose up -d --remove-orphans

                      echo "✅ Deployment complete"

                  post-deploy-command: |
                      cd ${{ env.PROJECT_PATH }}

                      echo ""
                      echo "📊 Service Status:"
                      docker compose ps --format "table {{.Name}}\t{{.Status}}\t{{.Ports}}" 2>/dev/null || docker ps --format "table {{.Names}}\t{{.Status}}"

                      echo ""
                      echo "✅ Deployment complete!"

            # ======================================================================
            # HEALTH CHECKS
            # ======================================================================
            - name: 🏥 Run Health Checks
              id: health-checks
              if: always() && steps.deploy.outcome == 'success'
              uses: nuniesmith/actions/.github/actions/health-check@main
              with:
                  # Note: health-check action expects SPACE-separated container names
                  containers: nginx photoprism nextcloud homeassistant audiobookshelf
                  ssh-host: ${{ secrets.FREDDY_TAILSCALE_IP }}
                  ssh-port: ${{ secrets.SSH_PORT || '22' }}
                  ssh-user: ${{ secrets.SSH_USER || 'actions' }}
                  ssh-key: ${{ secrets.SSH_KEY }}
                  initial-delay: 30
                  retry-count: 3
                  retry-delay: 10
                  fail-on-unhealthy: false

            # ======================================================================
            # DISCORD NOTIFICATION (Optional)
            # ======================================================================
            - name: 📣 Notify deployment
              if: always()
              uses: nuniesmith/actions/.github/actions/discord-notify@main
              continue-on-error: true
              with:
                  webhook-url: ${{ secrets.DISCORD_WEBHOOK_ACTIONS }}
                  title: "🏠 Freddy Deployment"
                  description: "Personal services server deployment"
                  status: ${{ steps.deploy.outcome == 'success' && 'success' || 'failure' }}
                  include-repo-info: true
                  fields: |
                      [
                        {"name": "Server", "value": "Freddy", "inline": true},
                        {"name": "Branch", "value": "${{ github.ref_name }}", "inline": true},
                        {"name": "Health", "value": "${{ steps.health-checks.outputs.healthy == 'true' && '✅ Healthy' || '⚠️ Check Required' }}", "inline": true}
                      ]

    # ==========================================================================
    # PIPELINE SUMMARY
    # ==========================================================================
    summary:
        name: 📊 Summary
        runs-on: ubuntu-latest
        timeout-minutes: 10
        needs: [dns-update, deploy]
        if: always()

        steps:
            - name: 📊 Generate summary
              run: |
                  echo "## 🏠 Freddy Deployment Summary" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "| Step | Status |" >> $GITHUB_STEP_SUMMARY
                  echo "|------|--------|" >> $GITHUB_STEP_SUMMARY

                  # DNS Update status
                  if [ "${{ needs.dns-update.result }}" = "success" ]; then
                    echo "| 🌐 DNS Update | ✅ Success |" >> $GITHUB_STEP_SUMMARY
                  elif [ "${{ needs.dns-update.result }}" = "skipped" ]; then
                    echo "| 🌐 DNS Update | ⏭️ Skipped |" >> $GITHUB_STEP_SUMMARY
                  else
                    echo "| 🌐 DNS Update | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
                  fi

                  # Deploy status (includes SSL generation)
                  if [ "${{ needs.deploy.result }}" = "success" ]; then
                    echo "| 🚀 Deploy (SSL + Services) | ✅ Success |" >> $GITHUB_STEP_SUMMARY
                  elif [ "${{ needs.deploy.result }}" = "skipped" ]; then
                    echo "| 🚀 Deploy (SSL + Services) | ⏭️ Skipped |" >> $GITHUB_STEP_SUMMARY
                  else
                    echo "| 🚀 Deploy (SSL + Services) | ❌ Failed |" >> $GITHUB_STEP_SUMMARY
                  fi

                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "### 📋 Details" >> $GITHUB_STEP_SUMMARY
                  echo "- **Server:** Freddy (Personal Services)" >> $GITHUB_STEP_SUMMARY
                  echo "- **Domain:** ${{ env.DOMAIN }}" >> $GITHUB_STEP_SUMMARY
                  echo "- **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
                  echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
                  echo "- **Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
                  echo "" >> $GITHUB_STEP_SUMMARY
                  echo "### 🔧 Architecture" >> $GITHUB_STEP_SUMMARY
                  echo "- All DNS records point to Freddy's Tailscale IP" >> $GITHUB_STEP_SUMMARY
                  echo "- Freddy's nginx reverse proxies Sullivan services over Tailscale" >> $GITHUB_STEP_SUMMARY
                  echo "- SSL certificates cover both Freddy and Sullivan subdomains" >> $GITHUB_STEP_SUMMARY

            - name: ❌ Fail on deployment error
              if: needs.deploy.result == 'failure'
              run: |
                  echo "::error::Deployment failed"
                  exit 1
